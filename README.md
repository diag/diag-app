steps for updating `diag-app`:

1) Do whatever makes sense, add tests, make sure you’re in good shape
2) `npm run build`
3) commit and push
4) update commit hash in `ui`
5) run `jet steps` if you wanna test before pushing